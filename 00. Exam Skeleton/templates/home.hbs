<div class="container">
    {{> header}}
        <h1>HOME!</h1>
        {{#if loggedIn}}
            <p>You are currently logged in. Your email is {{ email }}, Your ID is {{ userId }}</p>
        {{else}}
            <p>You are currently not logged in. <a href="#/login">Log in</a> / <a href="#/register">Register</a></p>
        {{/if}}
</div>
{{> footer}}